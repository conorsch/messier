.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install messier

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv messier
    $ pip install messier
